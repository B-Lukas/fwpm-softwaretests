stages:
- build
- test

image: maven:3.6.0-jdk-11-slim

build:
  stage: build
  script:
  - mvn clean install -DskipTests

junit:
  stage: test
  script:
  only: development
  - mvn clean verify
  artifacts:
    reports:
      junit:
      - target/surefire-reports/TEST-*.xml